export const selectMainPosts = (state) => state.post.mainPosts;
export const selectLoading = (state) => state.post.mainPosts.loading;
export const selectError = (state) => state.post.mainPosts.error;
